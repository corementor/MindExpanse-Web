import sigle from "../assets/imgs/singleAddition.jpg";
import twodigit from "../assets/imgs/2Digit.jpg";
export const images = {
  single: sigle,
  doublenum: twodigit,
}; // Exporting the image path from the assets folder as a named export. This will allow other files to import it using the `import {add} from "../assets/imgs/add.jpg";` syntax. This is useful for managing image assets in a larger project.
